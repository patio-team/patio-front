export default (client: {}) => ({
//   login (username: string, password: string) {
//     return client
//       .post('login/', {
//         username: username,
//         password: password
//       })
//       .then((response: any) => response.data.token);
//   }
});
