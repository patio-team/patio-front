.view.login
  h2.title Sign in

  form.form(data-testid="form")
    fieldset.username
      label email
      input(type="text" placeholder="type your email")

    fieldset.password
      label password
      input(type="password" placeholder="type your password")

    button.btn.submit Sign in

    a.forgot-password(tabindex="0") Forgot password?

  p Don't have an account #[a.sign-up(tabindex="0") Sign up]
